concourse:
  web:
    enabled: true
    externalUrl: "unjinxed.dev"
    # baseUrl: "/ci"
    containerPorts:
      http: 8081
  worker:
    enabled: true
  service:
    web:
      type: NodePort
      ports:
        http: 8080
  postgresql:
    auth:
      password: <+serviceVariables.postgresqlPassword>
  secrets:
    localUsers: <+serviceVariables.concourseUsername>:<+serviceVariables.concoursePassword>

  ingress:
    enabled: true
    hostname: "unjinxed.dev:8081"
    annotations:
        nginx.ingress.kubernetes.io/rewrite-target: /$2
        nginx.ingress.kubernetes.io/use-regex: "true"
        kubernetes.io/ingress.class: "nginx"
    extraRules:
    - host: "unjinxed.dev:8081"
      http:
        path: /ci(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: concourse-svc
            port: 
              number: 8080
